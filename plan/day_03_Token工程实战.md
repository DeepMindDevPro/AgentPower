
在 Day1 我们搭建了智能体的骨架，Day2 注入了推理的神经回路，而今天我们要直面让这一切得以运行的物质基础——**Token**。Token 既是智能体的运算原子，也是成本与能力边界的标尺。不理解 Token 工程，就无法让智能体在有限资源下持续可靠地运行。

---

## 一、Token 的本质

Token 是语言模型理解和生成文本的**最小语义单元**，不等同于“单词”或“字符”。一个 Token 可能是一个完整的常用词（如“the”），也可能是词的一部分（如“play”被拆成“pl”+“ay”），甚至是一个标点符号。

**分词器（Tokenizer）** 负责在文本与 Token 序列之间双向转换。主流模型通常使用 **字节对编码（BPE）** 或其变体：
- 先将每个字符视为独立 Token。
- 统计相邻 Token 对出现频率，将最高频的组合合并为新 Token。
- 反复合并直到达到预设词表大小（如 GPT-3 的 50257，GPT-4 的约 10 万）。
- 词表外的罕见词会被拆分为子词，**这也是中文和特殊字符通常比英文更“费 Token”的原因**——中文单字经常需要拆成字节序列再重组。

**理解 Token 的两条铁律**：
1. **模型的一切计算都基于 Token 序列**：输入被编码为 Token ID 序列，输出逐个解码回文本。
2. **计费和速率限制通常以 Token 为单位**：输入 Token 数 + 输出 Token 数决定了资源消耗。

---

## 二、Token 估算实战

### 1. 常用估算方法
- **在线分词器可视化**：OpenAI 官方 tokenizer 页面，直观粘贴长文本查看 Token 数和切割方式。
- **编程库**：
  - `tiktoken`：OpenAI 开源，支持 GPT-3/4 家族。
  - `transformers` 的 `AutoTokenizer`：可加载各类开源模型的分词器。
  - `sentencepiece`：Google 的多语言分词器。
- **粗略估计**（仅适用英文）：
  - 1 单词 ≈ 1.3 Token
  - 1 字符 ≈ 0.25 Token
  - 中文：1 个汉字 ≈ 1.6~2 Token（因模型而异）

**示例代码（tiktoken）**：
```python
import tiktoken
enc = tiktoken.encoding_for_model("gpt-4")
tokens = enc.encode("Hello, 世界！")
print(len(tokens))  # 输出 5
```

### 2. 特殊 Token 不可忽略
模型输入中会自动/手动插入控制 Token，如：
- `<|endoftext|>`：文本分隔符
- `<|im_start|>`, `<|im_end|>`：对话角色标识
- 填充、分隔等
这些都会占用上下文窗口，**实际可用输入长度 = 模型上下文窗口 - 预留特殊 Token 数量**。

### 3. 不同模型的差异
- GPT-4 Turbo（128K 上下文）：使用 cl100k_base 分词，中英混合效率较高。
- Claude 系列：使用自定义分词器，中文 Token 效率往往较高。
- Llama 2/3：基于 SentencePiece 的 BPE，词表更大，中文相对友好。

**实战心法**：永远在开发初期就用目标模型的分词器算一次精确 Token 数，不要依赖通用估算公式。

---

## 三、上下文窗口原理

### 1. 什么是上下文窗口
上下文窗口（Context Window）是模型一次能够“看到”并参与注意力计算的最大 Token 数量。它是模型在**训练时确定**的硬性限制，由位置编码和注意力矩阵的尺寸共同决定。

### 2. 注意力机制与长度约束
标准的自注意力（Self-Attention）计算复杂度为 \(O(n^2)\)，\(n\) 为序列长度。为控制计算量，模型在训练时设定一个最大序列长度，并让位置编码（如 RoPE、ALiBi）能够泛化支持该长度的位置标识。
- **训练长度 ≠ 最大推理长度**：部分模型通过微调或插值方法扩展上下文，但通常存在质量衰减。
- **窗口不是“滑动”的**：在生成每个新 Token 时，模型会重新计算整个上下文的注意力（或使用 KV 缓存）。但一旦输入序列总长度超过窗口，**超出部分的 Token 会被直接截断或压缩**，这部分信息彻底消失。

### 3. 上下文窗口的构成（以对话模型为例）
一次生成请求的完整输入通常包括：
```
系统提示语（System Prompt）
对话历史（User1 / Assistant1 / User2 / Assistant2 ...）
当前用户消息（User Message）
```
这三者的 Token 总和必须 ≤ 模型上下文窗口。

**关键点**：模型**没有记忆**，每次请求都是独立的。为了让模型“记住”前文，必须每次都将历史对话作为输入重新发送。这就是为什么多轮对话会**指数级吞噬上下文空间**。

---

## 四、超长对话超限的深层原因

### 1. 对话历史膨胀
每一轮对话都会往“历史”中添加：
- 用户消息（可能很长，含文档、代码）
- 模型回复（同样可能很长）
随着轮数增加，历史部分占据的 Token 数线性增长，最终挤占当前任务的空间。

**典型故障场景**：
- 第 1 轮：系统提示 100 Token + 用户 50 Token = 150 Token
- 第 10 轮：历史已累积 3000 Token，加上系统提示和当前输入，共 3200 Token
- 第 50 轮：超出窗口，最早的消息被**截断（通常丢弃开头的上下文）**，模型突然“失去记忆”，行为错乱。

### 2. 截断策略的副作用
当输入超出窗口时，API 或框架会自动截断：
- **丢弃最早的对话**：模型忘记初始指令和早期关键事实。
- **截断中间部分**：导致对话断裂，逻辑不连贯。
- **直接报错**：拒绝处理。

即使未报错，截断也是智能体的“脑损伤”：**规划依赖的历史事实消失，反思需要的因果链断裂**。

### 3. 推理范式加剧 Token 消耗
- **CoT/ToT** 在内部生成大量推理 Token，可能单次思考就用掉数千 Token。
- **ReAct** 的多次行动-观测循环，每次循环都记录进历史，极快撑大窗口。
- **Self-Refine** 多次迭代生成多个版本，占用更多 Token。
这意味着 Day2 所学的思维利器，若不加以 Token 管控，会加速“智力死亡”。

### 4. 位置编码的衰减
即使窗口能容纳，模型对于超长上下文中**位置靠中前部**的信息检索能力也会显著下降，此乃“中间丢失”（Lost in the Middle）现象。这进一步限制了有效上下文长度，即使在窗口内，也不是全部位置都同等可靠。

---

## 五、Token 工程在智能体架构中的落位

回到 Day1 的五组件架构，**Memory 模块**是 Token 管控的主战场：
- **工作记忆**应维护一个精简的上下文窗口：保留最近 N 轮完整对话、系统提示、当前任务状态。
- **长期记忆**（向量库、知识图谱）存储完整历史，按需检索提炼后注入工作记忆，而非全量带入。
- **规划模块**发出的推理链可以被压缩或摘要后存入记忆，而不是记录所有中间步骤。
- **反思模块**可生成结构化的经验总结，帮助记忆做“知识蒸馏”，减少冗余。

**实战原则**：
- **动态窗口管理**：实时计算当前请求的 Token 数，主动摘要/遗忘。
- **分级记忆**：热数据（最近对话）留在窗口；温数据（近期摘要）存储在短期缓存；冷数据（全量历史）归档到外部存储器。
- **成本意识**：每一次 ReAct 循环都分析 Token 消耗，避免无效行动。

---

## 小结

Token 工程是智能体从“概念验证”走向“可靠运行”的关键一役。今天你掌握了：
- **估算**：用工具精确计算，理解中文效率。
- **窗口原理**：硬性限制来自训练，注意力平方复杂度是根源。
- **超限原因**：历史堆叠、截断遗忘、推理膨胀、位置衰减。

明天当你面对一个多轮对话智能体突然“失忆”或“发疯”时，第一件要做的事情就是**计算它的上下文窗口利用率**，并检查记忆管理策略。这是所有高阶智能体稳定的基石。